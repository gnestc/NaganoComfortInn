%h1 Liste des rapports
%table
  %thread
    %tr
      %th Numéro du rapport
      %th{:colspan => "3"} Actions
  %tbody
    - @reports.each do |report|
      %tr
        %td= report.id
        %td= link_to 'Voir', admin_report_path(report, format: :pdf)
        %td= link_to 'Modifier', edit_admin_report_path(report)
        %td= link_to 'Supprimer', admin_report_path(report), method: :delete
%p
  = link_to 'Nouveau rapport', new_admin_report_path
